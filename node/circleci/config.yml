version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-2404:current

    steps:
      - checkout

      - restore_cache:
          keys:
            - npm-v1-{{ checksum "package-lock.json" }}
            - npm-v1-

      - run: wget -qO- https://api.wodby.com/v1/get/cli | sh
      - run: wodby ci init $WODBY_APP_SERVICE_ID

      - run:
          name: Install npm dependencies
          command: wodby ci run -v $HOME/.npm:/home/wodby/.npm -- npm ci

      - save_cache:
          key: npm-v1-{{ checksum "package-lock.json" }}
          paths:
            - ~/.npm

      - run: wodby ci build
      - run: wodby ci release
      - run: wodby ci deploy
# Skip post deployment scripts execution:
#      - run: wodby ci deploy --skip-post-deploy
