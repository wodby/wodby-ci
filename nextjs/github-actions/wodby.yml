name: Wodby Next.js Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v6

      - uses: wodby/actions/setup-wodby-cli@v1
        with:
          api-key: ${{ secrets.WODBY_API_KEY }}
          app-service-id: ${{ vars.WODBY_APP_SERVICE_ID }}

      - name: Install dependencies
        run: wodby ci run -s nextjs -- npm ci

      - name: Build image
        run: wodby ci build nextjs -f Dockerfile

      - name: Release image
        run: wodby ci release

      - name: Deploy
        run: wodby ci deploy
